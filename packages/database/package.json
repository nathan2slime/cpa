{
  "version": "1.0.0",
  "name": "@cpa/database",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "db:migrate:dev": "prisma migrate dev",
    "studio": "prisma studio",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:reset": "prisma reset",
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "license": "ISC",
  "dependencies": {
    "prisma": "^6.0.1"
  },
  "devDependencies": {
    "@prisma/client": "^6.0.1"
  }
}
